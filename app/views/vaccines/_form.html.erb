<%= form_for(@vaccine) do |f| %>

   <% if @vaccine.errors.any? %>
      <div id="error_explanation">
	 <h2><%= pluralize(@vaccine.errors.count, "error") %> prohibited this vaccine from being saved:</h2>

	 <ul>
	    <% @vaccine.errors.full_messages.each do |msg| %>
	       <li><%= msg %></li>
	    <% end %>
	 </ul>
      </div>
   <% end %>

   <table>
      <tr>
	 <td><%= f.label :name %></td>
	 <td><%= f.text_field :name %></td>
      </tr>
      <tr>
	 <td><%= f.label :no_of_doses %></td>
	 <td><%= f.text_field :no_of_doses %></td>
      </tr>

      <tr>
	 <td><%= f.label :doses_gaps %></td>
	 <td><%= f.text_field :doses_gaps %></td>
      </tr>


      <tr>
	 <td>
	    <div class="actions">
	       <%= f.submit %>
	    </div>
	 </td>
      </tr>
   </table>
<% end %>
